import React from "react";
import { createRoot } from "react-dom/client";
import SmartCaddiePrototype from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SmartCaddiePrototype />
  </React.StrictMode>
);
