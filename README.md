# Smart Caddie Loop iOS 18-hole prototype

A Vite React prototype ready for GitHub and Vercel.

## Vercel settings

- Framework Preset: Vite
- Build Command: npm run build
- Output Directory: dist
- Install Command: npm install

Make sure `package.json`, `index.html`, `vercel.json`, and `src` are at the top level of the GitHub repo.
